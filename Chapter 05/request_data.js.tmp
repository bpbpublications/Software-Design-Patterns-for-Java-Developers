
m.on('data', (d) => {
  // do work on d
  // (arbitrary type,
  // arbitrary length)
})

const data = ''
m.on('data', (d) => {
  data += d
)}

m.on('end', () => {
  // work on data
  // (complete data)
})

