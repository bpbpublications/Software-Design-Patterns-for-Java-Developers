const h = require('http')
const f = require('fs')
const s = h.createServer((q, r) => {
  h.get('http://localhost:13000/myservice', () => {
    const data = '';
    m.on('data', (d) => {
      data += d
    })
    m.on('end', () => {
      r.end(data.toString())
    })
  })
})
s.listen(12000)
