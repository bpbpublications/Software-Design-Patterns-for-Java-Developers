const h = require('http')

const r = h.request({path: '/foo',
                     port: 12000,
                     method: 'POST'}, (m) => {
  m.on('data', (d) => {
    console.log(d.toString())
  })
})
r.end()
