const h = require('http')
const f = require('fs')
const s = h.createServer((q, r) => {
  const file = q.url.substring(1)
  const data = f.readFileSync(file)
  r.end(data.toString())
})
s.listen(12000)
